{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist/",
    "baseUrl": ".", //to use webpack aliases
    "paths": {
      "@/*": ["./src/*"] //to use webpack aliases
    }
  }
}
